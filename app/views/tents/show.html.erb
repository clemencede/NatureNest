<h1 class="titremomo"><%= @tent.name %></h1>

<%# carousel %>
<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
     <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
      <img class="d-block w-100" src=<%= @tent.photo_url %> alt="First slide">
       <a class="btn btn-ghost" href="#"> <%= @tent.price %> </a>

    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src=<%= @tent.photo_url %> alt="Second slide">
       <a class="btn btn-ghost" href="#"> <%= @tent.price %> </a>
       <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src=<%= @tent.photo_url %> alt="Third slide">
       <a class="btn btn-ghost" href="#"> <%= @tent.price %> </a>
       <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>


<%# <!-- card info --> %>
    <div class= "profilepicture">
            <img class="profilepicture" src=<%= @user.profilepic %> alt="Third slide">
            <div class= "profileinfos">
            <h2> <%= @tent.user.first_name%></h2>
            <p> <%= @tent.description%></p>
            </div>
              <div class="notification-actions">
      <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
</div>
    </div>
    